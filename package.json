{
  "name": "gulp_project",
  "version": "1.0.0",
  "description": "Prototype with same folder structure as the Epinova template",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LL",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.11.1",
    "express": "^4.13.4",
    "gulp": "^3.9.1",
    "gulp-sass": "^2.2.0"
  }
}
